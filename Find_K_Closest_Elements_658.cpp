#include<bits/stdc++.h>
using namespace std;

int main() {
    int n,ele,x,k;
    cin >> n >> k >> x;
    vector<int> arr;
    for(int i=0;i<n;i++) {
        cin >> ele;
        arr.push_back(ele);
    }
    
    return 0;
}