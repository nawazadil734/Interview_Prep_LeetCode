#include<bits/stdc++.h>
using namespace std;

int main() {
    int n,k;
    cin >> n >> k;
    vector<vector<int>> res;
    int i=0;
    vector<int> p(k,0);
    // while(i>=0) {
    //     p[i++];
    //     if(p[i] > n) --i;
    //     else if ( i == k - 1) res.push_back(p);
    //     else
    //     {
    //         ++i;
    //         p[i] = p[i-1];
    //     }
    // }
    
    return 0;
}