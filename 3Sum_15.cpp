#include<bits/stdc++.h>
using namespace std;

int main() {
    int n,x;
    cin >> n;
    vector<int> nums;
    for(int i=0;i<n;i++) {
        cin >> x;
        nums.push_back(x);
    }
    vector<vector<int>> res;
    vector<int> ans;
    for(int i=0;i<n;i++) {
        for(int j=i+1;j<n;j++) {
            
        }
    }
    return 0;
}